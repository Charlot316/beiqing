.scenicBody {
    width: 100%;
    height: 100%;
    position: absolute;
    background: white;
    .scenicContent{
        position:absolute;
        margin:20px;
        width:calc(100% - 40px);
        height: calc(100% - 40px);
        overflow: auto;
    }
}

